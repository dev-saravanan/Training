git config --global user.name "Saravanan K"
git config --global user.email "kvsaravanan716@gmail.com"

git remote add origin repo-url

git init
git add .
git commit -m "This is a commit message"
git push origin main

git branch -M main
git branch -a
git checkout branch-name

git pull

